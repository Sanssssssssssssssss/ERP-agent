# 模型回归结果

**2026-09-24：聊天范围与空付款条款修复，源码 `b7bc012`。** 默认聊天跟随当前业务；明确选择“整个会话”时保持无绑定，并提示选业务回读。Odoo `false` 表示已读取但未设置付款条款；缺字段、遮盖和拒读仍为未知。回退点 `conversation-observation-baseline-20260924`。

- 146 tests＋42 subtests 通过；桌面 typecheck、build、真实浏览器交互检查通过。S01499 当前权限下真实 Odoo 回读 8/8，16 次记录读取，无业务写入。
- D04 取自真实失败请求，基线与候选各一次。候选正确要求选择业务，不虚构执行状态、不建议重跑；基线只追加合法发票读取，未复现历史错误断言，单轮未完成回答。
- **32,347 tokens**：新输入27,087、缓存4,608、输出652（含reasoning392）。实际工具执行0、重试0。仅证明该节点行为与回读机制；未重跑完整业务。

[模型上下文、响应与审阅](../../.runtime/agent-regression-scope-20260924/summary.json) · [真实 Odoo 回读](../../.runtime/payment-term-readback-20260924/live-result.json) · [桌面检查](../../.runtime/chat-scope-fix-20260924/offline-checks.json)

**2026-09-24：三项局部修复通过验证，源码 `c6bc771`。** 回读只从任务绑定和已核验动作扩展；跨 run 发送先核对同发票/PDF/地址的历史回执；SOP 不再背书未核验的方法名。回退点 `runtime-guards-baseline-20260924`。

- 离线：253 tests＋50 subtests 通过，覆盖撤权、旧审批、PDF/地址变化、未知发送及核对恢复。真实 Odoo 新空账本查到 S01499 已投递，新增动作和发送写入均为 0。
- 真实模型共 4 次：A04 基线再次误调用 `confirm`；候选改查 `action_confirm` SOP，下一步纠正。既有 B02 合法发送意图、B03 SMTP 回读不重发均通过结构检查与逐条审阅。
- 总计 **94,476 tokens**：新输入8,221、缓存85,120、输出1,135（含reasoning634）；3个工具意图、实际执行0、compaction0、重试0、用量未知0。

只新增有真实故障的 A04；UI 后处理和静态发现的跨 run 边界不伪造成付费 trace。B02/B03 保留原上下文，不能据此声称验证了新 runtime 的完整业务。尚未解决跨进程同时发送竞态、其他账本中完全无 Odoo 痕迹的未知投递；付款条款空值、普通聊天范围是已登记的独立问题。

[本轮汇总与审阅](../../.runtime/agent-regression-guards-20260924/summary.json) · [真实 Odoo 回执](../../.runtime/agent-regression-guards-20260924/live-odoo-final/result.json) · [离线检查](../../.runtime/agent-regression-guards-20260924/offline-tests.txt) · [案例维护规则与待修索引](README.md)

**2026-09-23：完整业务记录。**

**四条完整业务已运行，业务状态检查通过；整体验收待审。** 冻结源码 `f32cc60`，DeepSeek v4 Flash / high，Mem0 关闭，隔离数据库与测试 SMTP，每题一次，无整题重跑或新增运行/请求/输出时限。按业务状态、安全和效率判断，不匹配固定工具顺序。

| 案例 | 实际结果 | 模型请求 | 已知 token | 参考变化 |
|---|---|---:|---:|---|
| S01499 | 确认→预付开票过账→发送，20/20；最后聊天未完成 | 60 | ≥915,381 | 历史有人工接管且双方均缺用量，不能精确比较 |
| 2003 | 官方100分，37项检查通过 | 22 | 747,302 | +11.01% |
| 2278 | 官方100分，38项检查通过 | 25 | 811,465 | **+25.52%，成本待审** |
| E01 | 部分收发货8/8；各行完成4件、欠单6件，未开票 | 18 | 258,898 | −6.55% |

合计 **125次模型请求、217次工具调用、17项账本动作 verified**。已知 **2,733,046 tokens**：新输入357,234、缓存2,268,416、输出107,396（其中reasoning81,872）；compaction 0。S01499最后一次HTTP 520用量未知，精确总量未知。S01499含查询、提案和聊天，三个执行业务本身为40次请求、716,071 tokens。

仍需处理的实测问题：

- **聊天范围**：普通讨论没有绑定业务ID，状态工具返回不可用，模型误说“只生成提案”。选中发送业务后读到了 `smtp_accepted`，最终答复遇到上游HTTP 520，未追加重试。聊天检查不算通过。
- **核验与展示**：已读到的空付款条款被误判成“未观测”，过账页面显示unknown；探索读取的旧发票混入卡片。确认摘要还把自动生成的待发货单说成“未创建”。实际落库和投递检查通过。
- **工具契约**：SOP将未核验的 `confirm` 输入转成执行指导，随后被runtime拒绝并自纠正。2003、2278各有8次工具错误，E01有2次只读字段错误，均在同一运行内恢复；错误请求已保留为局部回归切点。
- **2278开销**：7个未用诊断工具的定义携带21轮；第14轮11,692个reasoning token对应的历史又携带11轮。调用次数27→25，但上下文和输出变长。字符归因不等于精确token归因，单次比较不能证明代码因果。

S01499最后聊天之后重新检查了数据库和原始MIME：客户邮件及官方PDF各一份，金额5,159.58 CNY，附件哈希一致，无额外PDF收件人或重复投递。另有一封无PDF的内部指派通知。两条ERPBench的Harbor包装器有已知评分JSON格式异常；原始官方100分、规则明细和数据库快照均保留。桌面两题逐项审批，ERPBench沿用原bench-auto审批，不混算HITL覆盖。

[完整汇总与证据哈希](../../.runtime/agent-regression-full-20260923/full/summary.json) · [S01499](../../.runtime/agent-regression-full-20260923/full/S01499/summary.json) · [两题官方报告](../../.runtime/agent-regression-full-20260923/full/bench/reports/index.html) · [E01](../../.runtime/agent-regression-full-20260923/full/E01/supervisor-review.json) · [2278成本审查](../../.runtime/agent-regression-full-20260923/full/bench/2278-cost-review.json)

此前局部回归：`0b08fb2`的12节点A/B共24次请求、512,868 tokens；`1d39ddd`的阶段/system/供给追加对照共10次、401,311 tokens。E02单轮输入119,128→13,538（−88.6%）；此次四条完整业务不代替E02制造全链路验收。B01单轮原分支也恢复正常，不能单凭样本宣称稳定修复；C02的真实宿主会持久化后暂停，不强制续出模型回复。

[24次局部记录](../../.runtime/agent-regression-20260923/summary.json) · [10次追加记录](../../.runtime/agent-regression-context-20260923/summary.json) · [运行入口与案例索引](README.md)。回退点保留 `regression-baseline-20260923`（`40e7fc6`）；本轮运行期间未修改生产源码，尚未晋级验收版本。
