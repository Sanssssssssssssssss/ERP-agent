# Used only by integration/pi_odoo_runner.py when it is run directly.
LLM_API_KEY=
LLM_BASE_URL=
LLM_MODEL=
LLM_THINKING_TYPE=high

# Optional: needed only when Harbor's Docker build/runtime must use a host proxy.
ERP_HARBOR_PROXY=
